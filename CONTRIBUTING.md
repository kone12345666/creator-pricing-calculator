# 协作与提交规范

## 提交前

- 不提交真实飞书表格标识、App Secret、个人令牌、业务数据或截图；
- 不提交 `.env`、`.env.local`、日志和构建产物；
- 涉及计算逻辑或写回逻辑时，补充或更新对应测试；
- 执行 `npm run test` 与 `npm run lint`。

## 分支建议

- `main`：可交付、可部署版本；
- `feature/...`：新功能；
- `fix/...`：问题修复；
- `docs/...`：文档维护。

## 合并检查

合并前至少由一位熟悉业务流程的同事确认：计算结果、写回范围和飞书字段映射均符合预期。
